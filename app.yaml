name: mcp_images
type: gradio-space
sdk: gradio

metadata:
  space_id: Chris4K/mcp_images
  original_url: https://huggingface.co/spaces/Chris4K/mcp_images

deploy:
  targets:
    - public-repo
    - huggingface

build:
  command: bash build.sh

test:
  command: echo "Tests not yet implemented"
